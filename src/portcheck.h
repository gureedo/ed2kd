#ifndef PORT_CHECK_H
#define PORT_CHECK_H

/**
  @brief start client port test
  @param client    source binary buffer
  @return 0 on success, -1 on failure
*/
int client_portcheck_start( struct e_client *client );

#endif // PORT_CHECK_H
